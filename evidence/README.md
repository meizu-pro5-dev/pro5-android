# Evidence

Place build logs, manifest snapshots, image hashes, boot logs, and device test
records here. Large evidence files are intentionally ignored by Git; concise
index files and conclusions should be committed under `docs/` or `locks/`.
