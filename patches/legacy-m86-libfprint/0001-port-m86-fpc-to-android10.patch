diff --git a/core.c b/core.c
index 7b75d72386b02efe89e0b81633741a6b4e1786e2..0b6d59110e9818138392b2eea8e3b2eb65327f38 100644
--- a/core.c
+++ b/core.c
@@ -305,7 +305,7 @@ void fpi_log(enum fpi_log_level level, const char *component,
 	}
 
 	va_start (args, format);
-	vsprintf(buff, format, args);
+	vsnprintf(buff, sizeof(buff), format, args);
 	va_end (args);
 
 	ALOGD("%s:%s [%s] %s", component ? component : "fp", prefix, function, buff);
@@ -346,7 +346,10 @@ static void register_drivers(void)
 static struct fp_driver *find_supporting_driver()
 {
 	GSList *elem = registered_drivers;
-	
+
+	if (!elem)
+		return NULL;
+
 	struct fp_driver *drv = elem->data;
 	fp_dbg("driver %s",drv->name);
 	return drv;
@@ -388,8 +391,10 @@ API_EXPORTED struct fp_dscv_dev **fp_discover_devs(void)
 
 	struct fp_dscv_dev *ddev = discover_dev();
 
-	tmplist = g_slist_prepend(tmplist, (gpointer) ddev);
-	dscv_count++;
+	if (ddev) {
+		tmplist = g_slist_prepend(tmplist, (gpointer) ddev);
+		dscv_count++;
+	}
 
 	/* Convert our temporary GSList into a standard NULL-terminated pointer
 	 * array. */
@@ -1137,4 +1142,3 @@ API_EXPORTED void fp_exit(void)
 	g_slist_free(registered_drivers);
 	registered_drivers = NULL;
 }
-
diff --git a/data.c b/data.c
index 1cf074f4e193480013215dcd05a12b93dec07621..de9b6db732c7c39ee93bb9e954203632b9ce1d9f 100644
--- a/data.c
+++ b/data.c
@@ -49,16 +49,31 @@
 
 static char *base_store = NULL;
 
-static void storage_setup(char *fp_path)
+static int storage_setup(const char *fp_path)
 {
-	base_store = g_build_filename(fp_path, ".fprint", NULL);
-	g_mkdir_with_parents(base_store, DIR_PERMS);
-	/* FIXME handle failure */
+	char *new_store;
+
+	if (!fp_path || !*fp_path)
+		return -EINVAL;
+
+	new_store = g_build_filename(fp_path, ".fprint", NULL);
+	if (!new_store)
+		return -ENOMEM;
+	if (g_mkdir_with_parents(new_store, DIR_PERMS) < 0) {
+		int error = errno;
+		g_free(new_store);
+		return -error;
+	}
+
+	g_free(base_store);
+	base_store = new_store;
+	return 0;
 }
 
 void fpi_data_exit(void)
 {
 	g_free(base_store);
+	base_store = NULL;
 }
 
 #define FP_FINGER_IS_VALID(finger) \
@@ -278,8 +293,9 @@ API_EXPORTED int fp_print_data_save(struct fp_print_data *data,
 	size_t len;
 	int r;
 
-	if (!base_store)
-		storage_setup(fp_path);
+	r = storage_setup(fp_path);
+	if (r < 0)
+		return r;
 
 	fp_dbg("save %s print from driver %04x", finger_num_to_str(finger),
 		data->driver_id);
@@ -384,8 +400,9 @@ API_EXPORTED int fp_print_data_load(struct fp_dev *dev,
 	struct fp_print_data *fdata;
 	int r;
 
-	if (!base_store)
-		storage_setup(fp_path);
+	r = storage_setup(fp_path);
+	if (r < 0)
+		return r;
 
 	path = get_path_to_print(dev, finger);
 	r = load_from_file(path, &fdata);
@@ -624,8 +641,8 @@ API_EXPORTED struct fp_dscv_print **fp_discover_prints(char *fp_path)
 	struct fp_dscv_print **list;
 	unsigned int i;
 
-	if (!base_store)
-		storage_setup(fp_path);
+	if (storage_setup(fp_path) < 0)
+		return NULL;
 
 	dir = g_dir_open(base_store, 0, &err);
 	if (!dir) {
@@ -747,4 +764,3 @@ API_EXPORTED int fp_dscv_print_delete(struct fp_dscv_print *print)
 	/* FIXME: cleanup empty directory */
 	return r;
 }
-
diff --git a/fpc1150.c b/fpc1150.c
index 2a54d94b5542ec6d8b526c93ee63e3995681466d..6bc9713a55499954be058d27b8e5191509d33b06 100644
--- a/fpc1150.c
+++ b/fpc1150.c
@@ -59,7 +59,7 @@
 #define FP_PXL_CTRL "/sys/devices/14d70000.spi/spi_master/spi4/spi4.0/setup/pxl_ctrl"
 
 /* sensor mode */
-int current_mode = -1;
+static int current_mode = -1;
 
 void contrast(unsigned char *src, int width, int height, int contrast)
 {
@@ -140,11 +140,11 @@ static inline void flip_v(unsigned char *src, int width, int height)
     }
 }
 
-int write_sysfs(char *sys_node, char *val)
+static int write_sysfs(const char *sys_node, const char *val)
 {
     int fd,ret;
 
-    fd = open(sys_node, O_WRONLY);
+    fd = open(sys_node, O_WRONLY | O_CLOEXEC);
         if (fd < 0) {
             fp_dbg("Failed opening %s node: %d", sys_node, fd);
             return -1;
@@ -153,6 +153,7 @@ int write_sysfs(char *sys_node, char *val)
     ret = write(fd, val, strlen(val));
         if (ret <= 0) {
             fp_dbg("Failed writing %s node: %d", sys_node, ret);
+            close(fd);
             return -1;
         }
     close(fd);
@@ -160,14 +161,13 @@ int write_sysfs(char *sys_node, char *val)
 }
 
 
-int reset()
+static int reset(void)
 {
     int fn,ret = 0;
-    int attempts = 3;
 
 #define FPC_HW_RESET    _IOW('K', 0, int)
 
-    if((fn=open(FPC_DEV, O_RDONLY)) < -1) {
+    if((fn=open(FPC_DEV, O_RDONLY | O_CLOEXEC)) < 0) {
                 fp_dbg("Error, can't open %s\n", FPC_DEV);
 		return -1;
 	}
@@ -202,12 +202,12 @@ static void nav_mode()
 }
 
 
-int get_raw_img(gchar *image, int size)
+static int get_raw_img(gchar *image, int size)
 {
-    int fn,ret;
+    int fn;
     int attempts = 3;
 
-    if((fn=open(FPC_DEV, O_RDONLY)) < -1) {
+    if((fn=open(FPC_DEV, O_RDONLY | O_CLOEXEC)) < 0) {
                 fp_dbg("Error, can't open %s\n", FPC_DEV);
 		return -1;
 	}
@@ -217,43 +217,46 @@ gri_again:
 	    attempts--;
 	    if(attempts!=0)
 		goto gri_again;
-	    else
+	    else {
+		close(fn);
 		return -1;
+	    }
 	}
     close(fn);
 
     return 0;
 }
 
-int get_raw_img_nonblock(gchar *image, int size)
+static int get_raw_img_nonblock(gchar *image, int size)
 {
     int fn,ret;
     int cycles = 10;
 
-    if((fn=open(FPC_DEV, O_RDONLY | O_NONBLOCK)) < -1) {
+    if((fn=open(FPC_DEV, O_RDONLY | O_NONBLOCK | O_CLOEXEC)) < 0) {
                 fp_dbg("Error, can't open %s\n", FPC_DEV);
             return -1;
         }
 
-    do {
+    while (cycles-- > 0) {
         usleep(100000);
         ret = read(fn,image,size);
-        if(ret == size)
-            break;
-        cycles--;
-        if(!cycles){
+        if(ret == size) {
+            close(fn);
+            return 0;
+        }
+        if (ret < 0 && errno != EAGAIN && errno != EINTR) {
+            int error = -errno;
             close(fn);
-            return FP_TIMEOUT;
-            }
-        } while(ret == -1 || !ret );
+            return error;
+        }
+    }
 
     close(fn);
-
-    return 0;
+    return FP_TIMEOUT;
 }
 
 /* non blocking wait for finger down */
-int wait_finger()
+static int wait_finger(void)
 {
     int fn,ret;
     int cycles = 10;
@@ -262,29 +265,29 @@ int wait_finger()
     fp_dbg("waiting for finger..\n");
 
     write_sysfs(FP_CAP_MODE, "7");
-    if((fn=open(FPC_DEV, O_RDONLY | O_NONBLOCK)) < -1) {
+    if((fn=open(FPC_DEV, O_RDONLY | O_NONBLOCK | O_CLOEXEC)) < 0) {
                 fp_dbg("Error, can't open %s\n", FPC_DEV);
                 return -1;
     }
-    do {
+    while (cycles-- > 0) {
         ret = read(fn,buf,4);
-        usleep(150000);
-        if(ret == 4)
-            break;
-        cycles--;
-        if(!cycles){
+        if(ret == 4) {
+            write_sysfs(FP_CAP_MODE, "1");
             close(fn);
-            return FP_TIMEOUT;
-            }
-        } while(ret == -1 || !ret );
-
-    write_sysfs(FP_CAP_MODE, "1");
+            return 0;
+        }
+        if (ret < 0 && errno != EAGAIN && errno != EINTR) {
+            int error = -errno;
+            close(fn);
+            return error;
+        }
+        usleep(150000);
+    }
     close(fn);
-
-    return 0;
+    return FP_TIMEOUT;
 }
 
-int wait_finger_up()
+static int wait_finger_up(void)
 {
     int fn,ret = 0;
     int cycles = 3;
@@ -293,29 +296,29 @@ int wait_finger_up()
     fp_dbg("waiting for finger up..\n");
 
     write_sysfs(FP_CAP_MODE, "8");
-    if((fn=open(FPC_DEV, O_RDONLY)) < -1) {
+    if((fn=open(FPC_DEV, O_RDONLY | O_CLOEXEC)) < 0) {
                 fp_dbg("Error, can't open %s\n", FPC_DEV);
                 return -1;
     }
 
-    do {
+    while (cycles-- > 0) {
         ret = read(fn,buf,4);
-        usleep(10000);
         if(ret == 4)
             break;
-        cycles--;
-        if(!cycles){
+        if (ret < 0 && errno != EAGAIN && errno != EINTR) {
+            int error = -errno;
             close(fn);
-            break;
-//            return FP_TIMEOUT;
-            }
-        } while(ret == -1 || !ret );
+            return error;
+        }
+        usleep(10000);
+    }
 
 
     write_sysfs(FP_CAP_MODE, "7");
+    close(fn);
     fp_dbg("waiting for finger up done..\n");
 
-    return ret;
+    return ret == 4 ? 0 : FP_TIMEOUT;
 }
 
 static int mode(struct fp_img_dev *dev, int sensor_mode)
@@ -343,7 +346,11 @@ static gint capture(struct fp_img_dev *dev, gboolean unconditional, struct fp_im
 	struct fp_img *img = NULL;
 	gchar *image, *image_tmp;
 
-	int fn, r = 0;
+	int r = 0;
+
+	if (!ret)
+		return -EINVAL;
+	*ret = NULL;
 
 	if(!unconditional){
     	    if((r = wait_finger())!=0)
@@ -352,18 +359,26 @@ static gint capture(struct fp_img_dev *dev, gboolean unconditional, struct fp_im
 
         image  = g_malloc0(IMAGE_SIZE);
         image_tmp  = g_malloc0(IMAGE_SIZE);
+	if (!image || !image_tmp) {
+		r = -ENOMEM;
+		goto out;
+	}
 
         if(unconditional == 1)
             r = get_raw_img(image,RAW_IMAGE_SIZE);
         else
             r = get_raw_img_nonblock(image,RAW_IMAGE_SIZE);
 
-        if(r == FP_TIMEOUT && r < 0){
+        if(r != 0){
             fp_dbg("no data acquired\n");
             goto out;
         }
 
         img = fpi_img_new_for_imgdev(dev);
+	if (!img) {
+		r = -ENOMEM;
+		goto out;
+	}
 
         rot90ccw(image, image_tmp, RAW_IMAGE_HEIGTH, RAW_IMAGE_WIDTH);
         memcpy(img->data, image_tmp, RAW_IMAGE_SIZE);
